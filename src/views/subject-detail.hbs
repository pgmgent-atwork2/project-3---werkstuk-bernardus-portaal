{{> up-nav}}
<article>
    <div class="page_title">
        <h1>{{subject.name}}</h1>
        <p class="right_title_teacher">{{subject.teacher.firstname}} {{subject.teacher.lastname}}</p>
    </div>
</article>